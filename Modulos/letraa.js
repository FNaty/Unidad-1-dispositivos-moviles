export const Mayusculas = (texto) => texto.toUpperCase();
export const Minusculas = (texto) => texto.toLowerCase();
export const LasDos = (texto) => texto.charAt(0).toUpperCase() + texto.slice(1).toLowerCase();